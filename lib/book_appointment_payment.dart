import 'package:flutter/material.dart';

class BookAppointmentPayment extends StatelessWidget {
  const BookAppointmentPayment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Maryam_Book Appointment Payment',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}